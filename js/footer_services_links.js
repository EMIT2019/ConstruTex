/*let fence = document.getElementById('fence_service');
let painting = document.getElementById('painting_service');
let remodeling = document.getElementById('remodeling_service');
let header_sec = document.getElementById('header_section');

const move_scroll = (dom_element, added_offset) => {
    window.scroll({
        top: dom_element.offsetTop - (added_offset.scrollHeight + 10),
        left: 0,
        behavior: "smooth"
    });
};

fence.addEventListener('click', () => {
    move_scroll(fence_pictures, header_sec);
    open_galery_slider(fence_item, false);
});

remodeling.addEventListener('click', () => {
    move_scroll(remodeling_pictures, header_sec);
    open_galery_slider(remodeling_item, false);
});


painting.addEventListener('click', () => {
    move_scroll(painting_pictures, header_sec);
    open_galery_slider(painting_item, false);
});*/